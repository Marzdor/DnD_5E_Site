.header {
  @mixin atSmall {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-gap: 5px;
    &-button {
      border: 1px solid black;
      background-color: transparent;
      padding: 5px;
    }
  }
}
